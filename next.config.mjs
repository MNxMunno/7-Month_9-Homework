/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.facebook.com",
      "twitter.com",
      "www.instagram.com",
      "ru.linkedin.com",
    ],
  },
};

export default nextConfig;
